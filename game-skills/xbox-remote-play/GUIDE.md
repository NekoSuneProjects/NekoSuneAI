# Xbox Remote Play planner guide

This package controls only an already authenticated, foreground Xbox app Remote
Play window on Windows. It does not store Microsoft credentials, wake a console,
launch arbitrary titles or bypass account/device protections. Keep the package
prohibited until a game-specific copy is created with reviewed controls and a
single-player or explicitly permitted multiplayer policy.
