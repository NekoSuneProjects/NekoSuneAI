# PlayStation Remote Play planner guide

This package controls only an already authenticated, foreground official or
compatible PlayStation Remote Play window on Windows. Controller compatibility
depends on the installed Remote Play client and driver. It never stores Sony
credentials, modifies the console, bypasses pairing/device protection or claims
unsupported wake/launch actions. Make a reviewed game-specific copy before
allowing input.
